@echo off
chcp 65001 >nul

REM =============================
REM 002: CSVファイルの特定の行だけを表示する
REM -----------------------------
REM sample002.csvの2行目（データ1件目）だけを表示します。
REM =============================
REM for /fコマンドで1行目（ヘッダー）をスキップし、最初のデータ行だけを表示します。
REM ---------------------------------------------
REM 【初心者向け解説】
REM for /f は「ファイルやコマンドの出力を1行ずつ読み込んで処理する」ためのオプションです。
REM /f を付けない for は単純な数値やリストの繰り返しですが、/f を付けると「テキスト行の繰り返し」になります。
REM 
REM tokens は「行を区切り文字で分割したとき、どの部分（トークン）を変数に格納するか」を指定します。
REM tokens=* は「行全体を1つのトークンとしてすべて格納する」という意味です。
REM 
REM skip=1 は「最初の1行（ヘッダー）をスキップする」指定です。
REM 
REM 例：for /f "skip=1 tokens=*" %%a in (sample002.csv) do (...)
REM   → sample002.csv の2行目以降を1行ずつ %%a に格納して処理します。
REM   → 今回は goto で1回だけ echo して終了しています。

for /f "skip=1 tokens=*" %%a in (sample002.csv) do (
  echo %%a
  goto :end
)
:end
REM 一時停止して画面を確認できるようにします。
pause
